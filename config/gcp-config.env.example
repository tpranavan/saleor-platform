# Shared-VM tunnel config is Feed Me / legacy only.
# BOTO Cloud Run uses dedicated project boto-services + Neon + Upstash —
# see ../saleor/deploy/gcp-config.env.example (no tunnel required).

GCP_PROJECT_ID=feed-me-qa
GCP_ZONE=us-central1-a
GCP_VM_NAME=fortronx-qa-shared
GCP_SSH_USER=oshirotechnologiesweb
